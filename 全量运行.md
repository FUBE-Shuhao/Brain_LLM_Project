### 5.16

全量投影中：

![image-20260516181303127](./../../Documents/BaiduSyncdisk/笔记/图片文件/image-20260516181303127.png)

gpt2 large的第11层全部岭回归完毕。

![image-20260517005003642](./../../Documents/BaiduSyncdisk/笔记/图片文件/image-20260517005003642.png)

创建所有模型的文件夹结构

```
for model in llama alpaca vicuna-v0 vicuna-v1.1; do
    for size in 7B 13B 30B 65B; do
        # 并不是所有模型都有 30B/65B，这里做个安全过滤，只建你有的
        if [ -d "/media/i9a2/WindowsData/Wsh/Brain_LLM_Project/scaling_finetuning-main/model_attention/$model/$size" ]; then
            mkdir -p "$RESULT_ROOT/$model/$size"
        fi
    done
done

echo "✅ 历史结果已成功归档至：$RESULT_ROOT/gpt2/large/layer11/"
echo "📂 其他大模型的全量输出总仓库已整齐预建完毕！"
```





接下来开始全部模型的全套运行

创建run_universe.py

```
import os
import subprocess
import sys

# 1. 基础路径配置
PROJECT_ROOT = "/media/i9a2/WindowsData/Wsh/Brain_LLM_Project/scaling_finetuning-main"
ATTN_DIR = os.path.join(PROJECT_ROOT, "model_attention")

# 2. 扫描你硬盘里实际存在的大模型生态链
# 自动生成结构如：[('gpt2', 'large'), ('llama', '7B'), ('alpaca', '13B') ...]
model_tasks = []
if os.path.exists(ATTN_DIR):
    for model_name in os.listdir(ATTN_DIR):
        model_path = os.path.join(ATTN_DIR, model_name)
        if os.path.isdir(model_path):
            for size in os.listdir(model_path):
                size_path = os.path.join(model_path, size)
                if os.path.isdir(size_path):
                    model_tasks.append((model_name, size))

# 3. 设立隔离黑名单
EXCLUDE_SUBJS = [21, 52]

print("🚀 === 终极全量算力流水线准备就绪 ===")
print(f"📦 探测到待跑的大模型阵列: {model_tasks}")

# 🦾 开启 4 重无情大循环（受试者 -> 模型 -> 尺寸 -> 层数）
# 严格采用串行机制：跑完一个释放一次内存，14900K 永不卡死
for subj in range(1, 53):
    if subj in EXCLUDE_SUBJS:
        print(f"🚫 [隔离] 主动跳过黑名单受试者: sub-{subj:02d}")
        continue
        
    for name, size in model_tasks:
        # 动态探测该模型在你硬盘里到底有多少个层级的 .npy 文件
        specific_attn_dir = os.path.join(ATTN_DIR, name, size)
        layers = []
        for f in os.listdir(specific_attn_dir):
            if f.endswith('.npy') and 'layer' in f:
                # 从文件名如 "rb_p1_layer11.npy" 中精准抠出层数数字
                try:
                    layer_num = int(f.split('layer')[-1].split('.npy')[0])
                    layers.append(layer_num)
                except ValueError:
                    continue
        
        # 从第 1 层到最后一层升序排列
        layers.sort()
        
        print(f"\n🧠 正在全面扫描: 受试者 sub-{subj:02d} | 模型: {name} ({size}) | 共 {len(layers)} 层")
        
        for layer in layers:
            print(f"  ▶ 正在计算: sub-{subj:02d} -> {name}_{size} -> Layer {layer} ...")
            
            # 构建标准的外部调用指令
            cmd = [
                "python3", "heads_vs_fmri.py",
                str(subj), "base", name, size, "lh", str(layer)
            ]
            
            # 提交任务并静静等待该进程完全结束、释放全部内存
            result = subprocess.run(cmd, capture_output=True, text=True)
            
            # 容错监控：如果主程序报错了，记录下来但绝不中断流水线
            if result.returncode != 0:
                print(f"  ❌ 警告: sub-{subj} 在计算 {name}_{size} 层 {layer} 时发生异常!")
                print(f"  错误日志: {result.stderr.strip()}")

print("\n🏆🏆🏆 [神话级任务完成] 全样本、全模型、全层级脑图基础数据已全量倾泻至 4_Results！")
```

修改head_vs_fmri.py

```
import os
import sys
import pickle
import numpy as np
import pandas as pd
import nibabel as nib  #  必须加这个，不然没法读 .gii 文件
from scipy.stats import zscore, pearsonr
from sklearn.linear_model import RidgeCV
import warnings
import time
from joblib import Parallel, delayed

pd.options.mode.chained_assignment = None
from scipy.stats import ConstantInputWarning
warnings.filterwarnings("ignore", category=ConstantInputWarning)

DIR = '/media/i9a2/WindowsData/Wsh/Brain_LLM_Project/scaling_finetuning-main/'
os.chdir(DIR)

subj_id = int(sys.argv[1])
group = sys.argv[2]
name = sys.argv[3]
size = sys.argv[4]
hem = sys.argv[5]
layer = int(sys.argv[6])

if subj_id in [21, 52]:
    print(f" [动态熔断] 受试者 sub-{subj_id:02d} 已被列入科研黑名单，回归程序已安全终止。")
    sys.exit(0)
	
subj = 'sub-0%d' % subj_id if subj_id<10 else 'sub-%d' % subj_id
model_size_heads = {'gpt2_base':12,'gpt2_medium':16,'gpt2_large':20,'gpt2_xlarge':25,
'llama_7B':32,'llama_13B':40,'llama_30B':52,'llama_65B':64,
'alpaca_7B':32,'alpaca_13B':40,'vicuna_7B':32,'vicuna_7B':40,
'mistral_7B':32,'gemma_7B':16}
n_head = model_size_heads['%s_%s' %(name,size)]

rm_id = [142,143,147,148,152,153,154]
words = pd.read_csv('Analysis/words.csv')
words_list = pickle.load(open('Analysis/words_list.p', 'rb'))
snts_list = [31,31,28,28,30]
cums = np.cumsum(snts_list)

layer_attn = np.load('model_attention/%s/%s/rb_p1_layer%d.npy' %(name,size,layer))
attn = layer_attn.swapaxes(2,0)
X = np.array([np.concatenate(i,axis=0) for i in attn])
X = np.delete(X,rm_id,axis=1)

X = np.nan_to_num(zscore(X.flatten(),nan_policy='omit')).reshape(X.shape)
X_train,X_test = [],[]
for i in range(X.shape[1]):
	tril_idx = np.tril_indices(words_list[i],k=-1)
	if i<133:
		X_snt_train = X[:,i,:words_list[i],:words_list[i]]
		X_train.append(X_snt_train[:,tril_idx[0],tril_idx[1]])
	else:
		X_snt_test = X[:,i,:words_list[i],:words_list[i]]
		X_test.append(X_snt_test[:,tril_idx[0],tril_idx[1]])
X_train = np.concatenate(X_train,axis=1)
X_test = np.concatenate(X_test,axis=1)

surf=[]
events_lst=[]

#  已经去掉了多余的 e
events_base_dir = '/media/i9a2/WindowsData/Wsh/DeepPrep/input'

for article in range(1,6):
	run = 1
	event_file = f'{events_base_dir}/{subj}/func/{subj}_task-read_run-{run}_events.tsv'
	events = pd.read_csv(event_file, delimiter='\t')
	events = events.dropna().reset_index(drop=True)
	run_article = int(events.SentenceID[0].split('.')[1])
	
	while run_article != article and run < 5:
		run += 1
		event_file = f'{events_base_dir}/{subj}/func/{subj}_task-read_run-{run}_events.tsv'
		events = pd.read_csv(event_file, delimiter='\t')
		events = events.dropna().reset_index(drop=True)
		run_article = int(events.SentenceID[0].split('.')[1])
		
	events['article'] = [article]*len(events)
	events_lst.append(events)
	
	fmri_gii = nib.load('Data/fsaverage5/%s_task-read_run-%d_hemi-%s_space-fsaverage5_bold.func.gii' %(subj,run,hem))
	fmri_data = np.column_stack([arr.data for arr in fmri_gii.darrays])
	fmri_data = np.nan_to_num(zscore(fmri_data,axis=0,nan_policy='omit'))
	surf.append(fmri_data)

events = pd.concat(events_lst).reset_index(drop=True)
corr = np.zeros(10242)

def process_vertex(v,X_train,X_test,events,surf,words):
	y_train, y_test = [],[]
	for article in range(1,6):
		n_snts = int(words[words.SentenceID.str.match('t.0%d' %article)].SentenceID.iloc[-1].split('.')[-1])
		for i in range(1,n_snts+1):
			snt_id = 't.0%d.0%d' % (article,i) if i < 10 else 't.0%d.%d' %(article,i)
			event = events[events.SentenceID.str.match(snt_id)]
			sid = i-1 if article==1 else cums[article-2]+i-1   
			fmri_snt = np.zeros((words_list[sid],words_list[sid]))
			tril_idx = np.tril_indices(words_list[sid],k=-1)
			event = event.reset_index(drop=True) 
			for ind, e in event.iterrows():
				if ind < len(event)-1:
					row = int(e.CURRENT_FIX_INTEREST_AREA_ID)-1
					col = int(event.iloc[ind+1].CURRENT_FIX_INTEREST_AREA_ID)-1
					scan = int(np.ceil((event.iloc[ind+1].onset/1000+5)/0.4))
					while scan >= surf[article-1].shape[1]:
						scan -= 1
					fmri_snt[row,col] = surf[article-1][v,scan]
			if sid<133:
				y_train.extend(fmri_snt[tril_idx[0],tril_idx[1]])
			else:
				y_test.extend(fmri_snt[tril_idx[0],tril_idx[1]])
	y_train = np.array(y_train)
	y_test = np.array(y_test)
	model_train = RidgeCV(alphas=np.logspace(1,3,20)).fit(X_train.T,y_train)
	y_predict = X_test.T @ model_train.coef_
	corr[v],_ = pearsonr(y_predict,y_test)
	corr[v] = np.nan_to_num(corr[v])
	return corr[v]

start = time.time()
results = Parallel(n_jobs=-1)(
    delayed(process_vertex)(v,X_train,X_test,events,surf,words) for v in range(10242)
)
corr = np.array(results)
end = time.time()
elapsed = end - start
print(f'Time taken: {elapsed:.6f} seconds')

#  必须加这一段，否则算完了没地方保存会前功尽弃
save_dir = f'/media/i9a2/WindowsData/Wsh/Brain_LLM_Project/4_Results/{name}/{size}/layer{layer}'
os.makedirs(save_dir, exist_ok=True)

# 保存文件名，此时目录里已经有层数信息了，文件名保持规范即可
np.save('%s/%s_subj%d_corr%s.npy' %(save_dir, group, subj_id, hem), corr)
```

