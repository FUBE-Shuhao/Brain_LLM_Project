### 5.10

接下来将要进行regression，运行heads_vs_fmri.py文件，需要修改里面部分路径。

发现代码里处理的对象是.gii文件，然而预处理完成的文件是.nii.gz，需要转换。

预处理数据里没有events.tsv文件，应该在rawdata文件夹中。



下载nilearn

pip3 install nilearn





原py文件采用的是先将大脑转化为2d图像，然后在2d图像上分布网格点的方式来进行岭回归



利用以下代码来进行投影

```
import os
import nibabel as nib
import numpy as np
from nilearn import surface, datasets

print("🚀 正在加载 fsaverage5 标准皮层模板...")
fsaverage = datasets.fetch_surf_fsaverage('fsaverage5')

# 输入路径
base_dir = '/media/i9a2/WindowsData/Wsh/Brain_LLM_Project/3_BrainData_Y/output/sub-01/func'
# 输出路径（对齐原版代码的 Data 目录）
out_dir = '/media/i9a2/WindowsData/Wsh/Brain_LLM_Project/scaling_finetuning-main/Data/fsaverage5'
os.makedirs(out_dir, exist_ok=True)

# 分离式投影循环
for run in range(1, 6):
    nii_file = os.path.join(base_dir, f'sub-01_task-read_run-{run}_space-MNI152NLin2009cAsym_desc-preproc_bold.nii.gz')
    print(f"\n🧠 正在处理 Run {run} ...")
    
    for hemi, mesh in [('lh', fsaverage.pial_left), ('rh', fsaverage.pial_right)]:
        texture = surface.vol_to_surf(nii_file, mesh, interpolation='linear')
        # 强制转换为 float32 防止报错
        texture = texture.astype(np.float32)
        
        darrays = [nib.gifti.GiftiDataArray(texture[:, t]) for t in range(texture.shape[1])]
        gii_img = nib.gifti.GiftiImage(darrays=darrays)
        
        # 完全遵循原版代码的命名规范
        out_file = os.path.join(out_dir, f'sub-01_task-read_run-{run}_hemi-{hemi}_space-fsaverage5_bold.func.gii')
        nib.save(gii_img, out_file)
        print(f"  ✅ 成功生成: {out_file}")

print("\n🎉 5个独立 Run 投影完成！第一块拼图归位。")
```

代码生成了5个独立的gii文件，放置在工作文件的~/Data/fsaverage5中
